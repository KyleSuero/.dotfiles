eval "$(thefuck --alias)"
eval "$(thefuck --alias fuck)"

alias suspend="sudo pm-suspend"
alias update="sudo apt-get update && sudo apt-get upgrade && sudo apt-get dist-upgrade"
alias cleanem="sudo apt-get autoremove && sudo apt-get autoclean && sudo apt-get clean "

# alias wparit="sudo wpa_supplicant -iwlan0 -c/etc/wpa_supplicant/example.conf -Dwext"

